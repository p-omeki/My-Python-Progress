#Python Range
'''Works like a for loop with integer value declared, initialization and inc/dec ment
  denoted as "range()"
  The original format is range(start, stop, step).
  Start is where to start. Default is 0 (Optional)
  Stop is where to stop the execution (Mandatory)
  Step is the increment. Default is 1 (Optional)
'''
#An example of range() below;

for a in range(1, 11, 1):
print (a)